{%- if not is_storage %}
---
apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: {{ sanitized_name }}
  namespace: {{ namespace }}
  labels:
    envId: {{ environment_id }}
    appId: {{ id }}
    app: {{ sanitized_name }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      envId: {{ environment_id }}
      appId: {{ id }}
      app: {{ sanitized_name }}
{%- endif %}