crds:
  gatewayAPI:
    enabled: true
    channel: experimental
  envoyGateway:
    enabled: true

# Enable pre-install hook to remove ValidatingAdmissionPolicy that blocks experimental CRD installation
preInstallHook:
  enabled: true
