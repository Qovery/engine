@ModuleInfo { minPklVersion = "0.32.0" }
module qovery.engine.platform.qoveryClusterGateway.configuration.inputs

// q-core resolves this from the cluster before COMPILE. It must not appear as a Console input
// during VALIDATE, because the binding API intentionally forwards customer-provided inputs only.
compileInputs: List<String> = List("dns.managedDomain")
