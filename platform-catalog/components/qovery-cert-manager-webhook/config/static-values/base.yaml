# Source 1 — the public Qovery DNS webhook needs no registry or DNS credential value.
certManager:
  namespace: cert-manager
  serviceAccountName: cert-manager

replicaCount: 1
image:
  repository: public.ecr.aws/r3m4q3r9/cert-manager-webhook-qovery
  tag: "84a4b276"
  pullPolicy: IfNotPresent
imagePullSecrets:
  name: ""
  value: ""

updateStrategy:
  type: RollingUpdate
resources:
  limits:
    cpu: 100m
    memory: 96Mi
  requests:
    cpu: 100m
    memory: 96Mi
