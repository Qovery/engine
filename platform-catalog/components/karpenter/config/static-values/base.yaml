# Retain the upstream two-replica HA placement/topology policy and retain the legacy controller resource requests.
replicas: 2
controller:
  resources:
    requests:
      cpu: 100m
      memory: 1Gi
    limits:
      memory: 1Gi
