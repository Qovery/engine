dns:
  domain: *domainWildcard

certManagerNamespace: qovery

gateway:
  qoveryPublic:
    enableProxyProtocol: false

envoyProxy:
  qoveryPublic:
    provider:
      kubernetes:
        envoyService:
          annotations: {}

metrics:
  enabled: false
  podMonitor:
    enabled: false
