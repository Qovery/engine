apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: qovery-cluster-public-gateway-client-traffic-policy
  namespace: {{ .Values.namespace }}
spec:
  {{- $numHops := -1 }}
  {{- if .Values.qoveryPublic }}
    {{- if .Values.qoveryPublic.xforwardedFor }}
      {{- if hasKey .Values.qoveryPublic.xforwardedFor "numberTrustedHops" }}
        {{- $numHops = .Values.qoveryPublic.xforwardedFor.numberTrustedHops }}
      {{- end }}
    {{- end }}
  {{- end }}
  {{- if ge $numHops 0 }}
  clientIPDetection:
    xForwardedFor:
      numTrustedHops: {{ $numHops }}
  {{- end }}
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: qovery-cluster-public-gateway
  enableProxyProtocol: true

