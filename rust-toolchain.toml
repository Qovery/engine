[toolchain]
channel = "1.98.0"
# Required by `helper.sh lint`; rustup only installs them by default with the
# `default` profile, and CI images are built with the `minimal` one.
components = ["rustfmt", "clippy"]
