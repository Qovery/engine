image: qoveryrd/ci:de41566a5c431364933ac11b5f959e71d3d2e0b5

services:
  - docker:dind

stages:
  - build
  - release
  - set_ga

before_script:
  - test -z $KUBERNETES_PORT || export DOCKER_HOST=tcp://127.0.0.1:2375

wip-build:
  stage: build
  retry: 0
  script:
    - ./helper.sh build_image
  except:
    refs:
      - dev
      - master

dev-build:
  stage: build
  retry: 0
  script:
    - ./helper.sh build_image
  only:
    refs:
      - dev

dev-release:
  stage: release
  retry: 1
  script:
    - ./helper.sh new_release
  when: manual
  only:
    refs:
      - dev

master-build:
  stage: build
  retry: 0
  script:
    - ./helper.sh build_image
  only:
    refs:
      - master

master-release:
  stage: release
  retry: 1
  script:
    - ./helper.sh new_release
  only:
    refs:
      - master

master-set-ga:
  stage: set_ga
  retry: 2
  script:
    - ./helper.sh set_release_ga
  when: manual
  only:
    refs:
      - master
